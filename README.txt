Copy the contents into "D:" drive if possible.

If "D:" is not available, update the "setupandrun" bat file with the appropriate directory.

Run the "setupandrun" batch file.

Browse to http://localhost:4200/ in Chrome.

To run unit tests, open command prompt, navigate to product-store directory and run "ng test".